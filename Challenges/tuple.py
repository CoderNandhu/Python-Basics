# Find and print the maximum and minimum number from the tuple.

# Find and print the index of the first occurrence of number 8.

# Create a new tuple that contains only the unique elements (no duplicates).

# Reverse the tuple and print it.

numbers = (4, 7, 2, 8, 5, 7, 2, 8)

print(max(numbers))
print(min(numbers))
print(numbers.index(8))


reversed_tuple = numbers[::-1]
print(reversed_tuple)